import { StyleSheet } from 'react-native';

export const avatarStyles = StyleSheet.create({
  avatarBase: {
    alignItems: 'center',
    justifyContent: 'center',
    overflow: 'hidden',
    position: 'relative',
  },
});
